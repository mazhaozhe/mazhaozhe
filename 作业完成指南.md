# 论文查重作业 - 完整操作指南

> 适用于零基础新手，按顺序逐步操作即可。

---

## 第一步：配置 Git（只需做一次）

打开终端（在文件夹空白处右键 -> Git Bash Here），依次执行：

```bash
# 设置你的名字和邮箱（用你 GitHub 注册的邮箱）
git config --global user.name "你的名字"
git config --global user.email "你的GitHub邮箱@example.com"

# 验证设置成功
git config --global user.name
git config --global user.email
```

---

## 第二步：在 GitHub 上创建仓库

1. 打开 https://github.com 登录
2. 点击右上角 **+** 号 -> **New repository**
3. Repository name 填：`paper-check`（或你喜欢的名字）
4. 选择 **Public**（公开，助教要检查）
5. **不要**勾选 "Add a README file" 等选项
6. 点击 **Create repository**
7. 创建完成后，页面会显示仓库地址，类似：
   `https://github.com/你的用户名/paper-check.git`
   **复制这个地址**，后面要用

---

## 第三步：本地初始化 Git 并做第一次提交

在项目文件夹（3124004138）中打开 Git Bash，依次执行：

```bash
# 1. 初始化 Git 仓库
git init

# 2. 添加基本功能文件（第一次提交：核心功能）
git add main.py text_processor.py similarity_calculator.py
git add requirements.txt stop_words.txt

# 3. 提交（写清楚提交信息）
git commit -m "feat: 实现论文查重核心功能

- main.py: 命令行入口，接收3个文件路径参数
- text_processor.py: 文本预处理（jieba分词、去停用词、过滤标点）
- similarity_calculator.py: 余弦相似度计算
- requirements.txt: 依赖列表
- stop_words.txt: 中文停用词表"

# 4. 查看提交记录确认成功
git log --oneline
```

---

## 第四步：做第二次提交（扩展功能）

```bash
# 添加测试文件和文档（第二次提交：扩展功能）
git add tests/ conftest.py data/ README.md PSP表格.md

# 提交
git commit -m "test: 添加20个单元测试和项目文档

- 20个单元测试覆盖边界值、异常处理、性能等场景
- 测试覆盖率86%，核心模块92%-100%
- 添加README、PSP表格和测试样例数据"

# 查看提交记录
git log --oneline
```

---

## 第五步：推送到 GitHub

```bash
# 1. 关联远程仓库（把下面的地址换成你自己的）
git remote add origin https://github.com/你的用户名/paper-check.git

# 2. 推送到 GitHub
git push -u origin main

# 如果提示输入账号密码：
# - 用户名：你的 GitHub 用户名
# - 密码：不能用密码！需要用 Personal Access Token
#   获取方法：GitHub -> Settings -> Developer settings ->
#   Personal access tokens -> Generate new token -> 勾选 repo 权限
```

> 如果 push 时报错 `main` 分支不存在，试试：
> `git push -u origin master`（旧版 Git 默认分支名是 master）

---

## 第六步：运行测试并截图

```bash
# 1. 运行全部测试（截图这个结果）
python -m pytest tests/ -v

# 2. 运行覆盖率测试（截图这个结果）
python -m pytest tests/ --cov=. --cov-report=term-missing

# 3. 生成 HTML 覆盖率报告（在浏览器打开截图）
python -m pytest tests/ --cov=. --cov-report=html
# 然后打开 htmlcov/index.html 截图
```

> 把这些截图保存好，写博客时要放进去。

---

## 第七步：运行性能分析并截图

```bash
# 1. 运行性能分析
python -m cProfile -s cumulative main.py data/orig.txt data/orig_add.txt data/ans.txt

# 2. 把结果保存到文件
python -m cProfile -o profile.out main.py data/orig.txt data/orig_add.txt data/ans.txt
python -c "import pstats; pstats.Stats('profile.out').sort_stats('cumulative').print_stats(20)"

# 3. 截图控制台输出
```

> 截图保存好，博客里要展示性能分析图和消耗最大的函数。

---

## 第八步：写博客

博客需要包含以下 7 个部分（总分 60 分）：

### 8.1 首行放 GitHub 链接（3分）
```
作业GitHub链接：https://github.com/你的用户名/paper-check
```

### 8.2 PSP 预估表（6分）
- 把 PSP表格.md 中"预估耗时"列的内容放到博客里

### 8.3 计算模块接口的设计与实现过程（18分）
需要写清楚：
- 有几个类、几个函数，它们之间的关系
- 画一个流程图（可以用文字描述或截图）
- 算法的关键：余弦相似度
- 独到之处：多编码自适应、标点符号过滤

### 8.4 性能改进（12分）
- 放性能分析截图
- 指出消耗最大的函数
- 描述优化思路

### 8.5 单元测试展示（12分）
- 展示部分测试代码
- 说明测试函数的功能
- 说明构造测试数据的思路
- 放覆盖率截图

### 8.6 异常处理说明（6分）
每种异常写一个测试样例和对应场景：
- FileNotFoundError：文件不存在
- UnicodeDecodeError：编码无法识别
- 空文件处理：返回 0.00
- 零向量处理：返回 0.00

### 8.7 PSP 实际耗时（3分）
- 编码完成后，把 PSP表格.md 中"实际耗时"列填好
- 放到博客里

---

## 检查清单（提交前对照）

- [ ] 文件夹名为 3124004138
- [ ] GitHub 仓库已创建，代码已推送
- [ ] 至少 2 次 commit
- [ ] 博客首行有 GitHub 链接
- [ ] PSP 表格已填预估耗时
- [ ] PSP 表格已填实际耗时
- [ ] 有流程图/设计说明
- [ ] 有性能分析截图
- [ ] 有单元测试代码展示
- [ ] 有覆盖率截图
- [ ] 有异常处理说明
- [ ] main.py 可以通过命令行正常运行
- [ ] 输出格式正确（如 0.85，不是 85%）
- [ ] requirements.txt 已提交

---

## 常见问题

### Q: git push 报错 "Authentication failed"
A: GitHub 已不支持密码登录，需要用 Personal Access Token。
   设置路径：GitHub -> Settings -> Developer settings -> Personal access tokens -> Generate new token

### Q: git push 报错 "non-fast-forward"
A: 先执行 `git pull origin main --allow-unrelated-histories`，再 push

### Q: jieba 分词太慢怎么办
A: 首次运行 jieba 需要加载词典（约 1 秒），之后会缓存。正常使用不会超过 5 秒限制。

### Q: 测试运行报错 "No module named jieba"
A: 执行 `pip install jieba pytest pytest-cov`

### Q: git 提交时中文乱码
A: 执行 `git config --global core.quotepath false`
